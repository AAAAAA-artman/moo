# 这是一个简单的优化项目
## 而这是我的研究思路
首先我通过sklearn库的LinearRegression类建立多项式回归模型，可是效果不好。  
不是过拟合就是欠拟合，于是我使用了l2正则化模型。这个模型拟合的效果还不错。  
然后我使用scipy库的minimize方法进行梯度优化，起始点选择平均值。  
结果算法最优解小于已有数据的最大值。于是我将起始点改为中位数。还是不行。  
最后我又改用了多起点。效果仍然不好。于是我估计可能是多项式回归模型本身的缺陷。  
接下来我可能会去学神经网络模型，试试有没有更好的效果。